# 黑客松展示验收 · 2026-09-06

## 本次改动

- 重做宣传首页，使用真实图书馆场景预览与三个简洁入口。
- 增加独立展示空间、三分钟演示路线、当日日程/课程/考试/待办/材料样例、一键重置与退出。
- 展示模式文字工具直接执行本地规则和摘录，显示来源；对话沿用预设问题与回应，不伪装成联网 AI。
- 展示默认专注时长为一分钟，按真实时间结算；48/12 默契及学习习惯明确作为样例种子。
- 压缩笔记本场景留白与角色页空白，放大候选角色缩略图，保留底部四分之一光滑桌面和手机上下布局。
- 番茄钟工具仅在打开时挂载，避免隐藏页面持续绘制人物，并确保读取加载后的专注时长。
- 普通空间日程与纪要请求增加 20 秒超时及现有本地替代。
- 缓存更新至 `study-buddies-shell-v14`。未改变依赖、环境、锁文件或部署配置。

## 自动检查

- 规范检查、TypeScript 类型检查通过。
- 校园解析样例通过：课程 2、考试 1、成绩忽略。
- 110 项专项检查通过，覆盖原有数据迁移、备份、照片接口模拟、行为与序列帧，以及本次新增的展示隔离、跨年日期、重复导入、一分钟真实结算和本地摘录。
- 正式构建、差异空白检查通过；公开目录和构建产物不含临时验收页面。

## 浏览器实测

在转发到相同开发服务的隔离来源 `localhost:3001` 运行 31 项检查，全部通过：

1. 展示数据种子、七页导航、1200×720 场景完整可见。
2. 首次人物开场、近期点击直接对话、物件活动独立于计时、再次点击完成。
3. 左日历右待办，周/月视图、课程/考试样例。
4. 文字日程识别来源标记、确认保存、重复导入去重。
5. 纪要本地摘要、加入共享待办、课件三段提纲。
6. 重置清除临时预览和展示结果。
7. 暂停后空座、计时冻结、继续返回；实际等待一分钟，记录恰好一分钟、默契 48→51、成长奖励可达。
8. 纯享进入与退出；390px 场景无横向溢出、日历待办上下排列；320px 首页无横向溢出。
9. 刷新保留专注记录；设置中的重新演示恢复种子；整轮操作后个人状态键内容逐字节不变。

另在原地址的展示空间目视检查：首页、浅色透明开场对话、女生场景、男生角色页与 12 点独立默契；缩小角色页留白后重新检查人物完整轮廓。390px 手机场景的房间、人物、三个物件及右上角纯享入口正常显示。未将浏览器尺寸模拟称作真实手机硬件验收。

## 边界

本次不重新生成艺术素材，沿用上轮 V3 透明序列帧。像素人物动作仍是现有完整姿势序列及共用衔接，未宣称照片生成一致性已解决。

真实图像服务、音频转写、教务登录与摄像头硬件效果不在本次已完成的现场主线验收中。主线已避开这些外部依赖；相关接口和替代入口保留。没有上线部署、账号或云同步。

回退标签：`pre-hackathon-showcase-20260906`。验收临时页在构建前移出公开目录，测试代理完成后关闭；原 `localhost:3000` 预览继续运行。
