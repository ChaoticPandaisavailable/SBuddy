# SBuddy 黑客松展示

## 打开

- 首页：http://localhost:3000/
- 独立展示空间：http://localhost:3000/?demo=1#play
- 人物动作预览：http://localhost:3000/?demo=1#animation-preview
- 互动与计分说明：http://localhost:3000/?demo=1#interaction-guide

网页端本地演示，不发布线上。保留当前依赖、环境与 3000 端口。已有依赖时，在 `site` 目录运行 `npm run dev`；已有服务正在运行时直接打开链接。

展示空间有独立的浏览器存储键 `sbuddy-showcase-v1`。个人空间仍用 `sbuddy-state-v2`。在设置中点击“重新演示”可恢复当天样例、清除展示进度，不清除个人记录或共享照片库；打开 `http://localhost:3000/#home` 回到个人空间。跨日展示前重新演示，让日期更新到当天。不要使用浏览器的“清除网站数据”来重置展示。右上角提供动画预览与互动说明，均为站内 hash 页面，并有返回按钮。

## 三分钟讲解

**0:00–0:25 · 介绍**

打开首页：“我们把日程管理、学习工具和像素角色陪伴放在同一张桌上，让完成计划也有情感反馈。”点击“开始体验”。说明这是预置样例空间；默契 48 和 12 以及小禾的两条学习习惯是展示样例，学习历史从零开始。

**0:25–0:55 · 陪伴与专注**

点击人物 → 点击开场文字推进 → “聊聊” → 点击问题推进 → 选择一个回答。问题、选项和回应分别占用底部对话框，回应再次点击后收起；空闲时没有对话框。Enter 或空格同样可推进。关闭后点击电子钟开始人物自习，同时展开右侧番茄钟，在这里开始一分钟专注。右侧工具默认隐藏：电子钟打开番茄钟，书本或电脑打开纪要。收起工具不丢失记录，也不会跳转到工具页。

**0:55–1:25 · 日程闭环**

进入“日常活动”→“日程识别”。材料已经备好，点击“识别日程”，校对后“确认写入日历”。展示周/月视图、右侧待办与课程/考试页。重复导入会去重。番茄钟离开游戏页后继续。

**1:25–2:00 · 学习工具闭环**

回到游戏，点击桌面电脑 → 右侧纪要“整理重点” → 把一个行动项加入待办。上课时点击书本也能打开同一份纪要。无需离开场景即可速记与整理；完整版工具页仍可从导航进入。时间充裕时打开工具中的课件整理，展示提纲与 Markdown 导出。速记使用本地规则和原文摘录，界面注明来源。

**2:00–2:35 · 成长与角色隔离**

回到游戏页，查看专注结算和左侧新待办。专注自然完成后记录真实的一分钟并加 1；提前结束保留时长但不加分。如果没有其他互动，初始 48 分完成一轮后是 49，不会提前解锁 50 分奖励。首次对话按选项 +1、+2 或 −1；鉴赏按 25、50、75、100、125、150 逐项解锁。角色页切到知序，说明外观、默契、对话各自独立，而学习资料属于用户。

自习和关联番茄钟去重：两轮正常专注共加 2，自习完成不再额外加 1。没有正常完成专注时，自习完成可加 1；手动活动须持续至少一分钟。课程、会议与其他日程各完成加 1，待办和整理纪要不加分。日程奖励只结算页面运行中观察到的完成，不补发导入历史。

**2:35–3:00 · 场景与收尾**

切换教室，角色离场后换景再返回；打开纯享模式，再用右上角入口或 Esc 退出。“学习不再只是勾掉任务，而是与一个记得你的搭子一起完成。”下一轮展示到设置中点“重新演示”。

## 备用演示

- 时间紧：先开始一分钟专注，再讲日历和纪要；不用等待录音或上传文件。
- 暂停演示离场：专注中点暂停，人物离开后保留空座；继续计时后返回。暂停时间不计入记录。
- 手机：展示空间仍可用；导航收进左上角菜单，日程与工具移到场景下方。
- 投影：浏览器保持 100% 缩放；建议 1280×720 或更宽，先打开首页与游戏页加载素材。
- 演示中修改样例后刷新，进度会保留。重置会中止当前展示计时并清除展示记录。

## 如实说明能力

已实现：两位默认序列帧人物、场景和桌面互动、角色独立对话/成长、日历/课程/考试/待办、真实专注计时、本地文本整理、备份恢复。

展示路径不依赖 AI 密钥。普通空间仍保留 AI 接口；未配置或失败时，文字整理使用明确标注的本地替代。展示空间的对话使用现有预设问题与回应。

照片生成接口及素材校验已接入，但真实照片生成、语音转写、截图识别需要有效服务，尚未验收真实生成一致性。实时听写取决于浏览器支持和权限；校园采集需要用户自行教务登录。手势需要摄像头，适合作为额外展示，不作为主线前置条件。首次加载或首次准备手势资源仍可能需要网络；不宣称完全离线安装。

回退点：`pre-hackathon-showcase-20260906`。它保存本次优化前的代码和素材快照，不包含密钥；当前分支和原有暂存区保持原状。
