# SBuddy 本地验收记录

验收日期：2026-09-05。环境：Windows、Node.js 25.8、Chromium（Codex 内置浏览器）。

## 命令检查

- npm run lint：通过。
- npx tsc --noEmit：通过。
- npm run build：通过，七个已有 AI API 路由均构建成功。
- npm run test:campus：通过，2 门课程、1 门考试，成绩忽略。
- npm run test:sbuddy：34 项通过。

专项测试覆盖新用户空状态、跨月跨年、闰年、中文下午时间、未知日期与时间补全、重复导入、角色关系隔离、异步结果归属、专注暂停/后台期限/提前结束/零时长/幂等结算、各档解锁、旧数据迁移与重新加载、损坏备份、待办完成映射、无密钥、服务异常与 IndexedDB 不可用。

构建提示包括 Vinext beta 尚不能静态分类首页路由和插件耗时提示；构建退出码为 0。npm ci 提示原依赖树有 11 项审计问题，本次未执行可能破坏版本兼容性的自动大版本升级。

## 浏览器实测

- 七页导航、工具返回、手机折叠菜单。
- 390×844 手机视口的角色纵向布局、月历及导航；无横向溢出。桌面宽屏检查主页、游戏和鉴赏。
- 创建“测试搭子”，聊天后默契 0→3，独立保存温柔提醒偏好；切换知序仍为 0；刷新后恢复原角色进度。
- 纯享模式进入、Esc 退出以及退出后导航。发现原生全屏未退出的问题后修复并复测。
- 文字识别“明天 14:00–16:00”，预览显示正确的完整日期；修改标题后确认，次日日历显示该安排。
- 待办新增、截止日期编辑保存、完成及恢复入口。修复日期控件兼容问题后确认 9/6 18:00 保存成功。
- 自定义 1 分钟专注、暂停、继续；切换页面及角色后自动完成，记录仍归属开始时的知序；可保存专注感受。
- 无密钥下文字纪要明确标记“本地速记”；行动项加入待办。
- 导出完整备份；损坏备份显示错误并保留原数据；含照片素材的有效测试备份显示恢复预览，确认后成功恢复。
- 通过测试备份验证 75 默契的六项奖励可见、像素击掌可播放，回看后默契仍为 75。此处使用隔离测试夹具，没有将测试数据作为真实学习历史。
- 浏览器错误/警告日志检查为空。
- 验收结束已通过设置清理本轮浏览器测试记录，恢复三位初始搭子与真实空状态。

## 需要真实环境补验

没有启用用户的真实摄像头、麦克风，也未登录教务系统或配置有效 AI 密钥。因此硬件权限拒绝的真人点击流程、实际音频转写、真实截图视觉准确率及登录后的采集桥往返没有端到端实测。相关错误分支保留明确提示、关闭媒体流与文本/按钮替代操作；无密钥和服务失败已由接口测试覆盖。

浏览器存储禁用/配额耗尽的提示路径已在代码中处理，IndexedDB 不可用有测试；没有破坏用户浏览器配置来强制造成真实 localStorage 配额耗尽。照片备份恢复已在实际 IndexedDB 验证。

本次没有公开部署。运行方法和功能边界见 README.md。

