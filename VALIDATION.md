> 2026-09-06 最新一轮：序列帧、四分之一桌面及日常活动分栏调整。实际结果见 [docs/SPRITE-V3-VALIDATION.md](docs/SPRITE-V3-VALIDATION.md)。以下记录保留为此前轮次的参考。

## 2026-09-06：桌面物件交互与自动行为（本次验收）

- 本轮替换游戏里的动作按钮、名称性格和默契条：左上指令切换与数字爱心，桌面三物件，底部浅色透明对话，右上纯享入口。完整人物和房间、左右学习组件继续使用上一轮实现。
- 规范检查、类型检查、校园解析测试、95 项 SBuddy 专项检查及正式构建通过。新增 9 项行为专项覆盖一小时边界、手动活动完成、区间合并、动作优先级、无上限默契、按角色备份及日程/待办/专注完成检测。
- 隔离来源 localhost:3001 的真实应用界面通过 26 项浏览器断言；测试使用原生文件输入预览和确认恢复，不直接替换应用内部状态。开始前留存原隔离数据及照片，结束后已恢复。
- 320×844 / 390×844 浏览器内嵌视口实测内容宽度 310 / 380（预留滚动条），无横向溢出；三个物件的点击区域都完整且至少 44×44。桌面 1280×720 查看场景、物件、爱心与透明对话；原生纯享模式实测 1707×1067，退出入口在右上可见。
- 电子钟进入手动自习但番茄钟仍未启动；再次点击完成后收到一次庆祝完成回调，随后回到待机。待办完成收到一次独立庆祝；快速连续点击最终进入会议；在独立动作演示页实际用 Enter 激活电脑，男女角色同时切到会议。
- 首次点击出现 Hello 开场，取消后再点击直接提问；人物显示问题时思考。点击刷新互动时间，刷新页面后仍能直接对话；超过一小时备份重新触发开场，一小时内备份直接对话。女角色互动后，男角色仍独立触发首次招呼。
- 回答使默契从 105 增至 108，没有触发庆祝；备份恢复 1005 默契保持原值。开场没有奖励；既有解锁节点保留。
- 番茄钟暂停后完成离场并保持空座，物件不可点；暂停期间换教室仍保持空座，继续计时后回座恢复操作。手机 CSS 纯享模式退出入口可见，Esc 能退出；桌面原生全屏进入和退出也实际操作。
- 恢复已结束的六小时日程不补播庆祝，直接进入疲惫待机；点击电子钟仍进入学习。真实等待下一分钟的日程结束边界后自动庆祝；倒计时自然结束后仅生成一条专注记录并奖励一次。
- V2 模拟照片人物备份恢复后 ready=true，外观、1005 默契和手动会议指令均保留；切换无版本旧图集角色后走 legacy-v1 读取并正常显示。缺少照片素材的损坏备份未出现确认入口，已有数据序列化内容不变。
- 浏览器确认 study-buddies-shell-v12 已启用。单元测试验证旧缓存清除及无关缓存保留；缓存版本不依赖更换访问端口。
- 验收辅助页面已移出 public，最终构建不含 __qa 文件；隔离代理与测试标签已关闭，原地址 localhost:3000 仍返回 200。320px 复查后，三个物件点击区均在画面内且互不重叠。
- 本轮未新增依赖、未修改锁文件、站点配置或环境配置；工作区此前已存在的配置与代码修改保留。Git 回退标签 pre-sbuddy-upgrade-20260905 仍存在。Flutter、线上发布均未涉及。
- 真实照片生成质量仍待服务配置；本轮未进行真机触控手感、媒体设备权限或教务账号登录测试。动画已通过代表场景实际播放和完成回调检查，没有把全部连接逐帧人工观看。

以下为此前各轮历史验收，不作为本轮新增通过项。

## 2026-09-06：满幅场景与 Q 版人物重做（上一轮记录）

- 预览维持 http://localhost:3000/#play，新增 http://localhost:3000/animation-preview 动作对照页；仅网页端，无发布或配置替换。
- 规范检查、类型检查、校园解析测试、86 项新版专项及正式构建通过。校园测试并行运行时出现测试热更新端口占用提示，退出码为 0，解析断言通过；正式构建保留既有路由分类提示。
- 新专项覆盖全部 100 组活动切换、根位置和起坐连续性、循环接头、快速改选、招呼/庆祝收尾、空座保持、手臂固定长度、落脚期足部位置、书写/抬笔及 V1/V2 备份兼容。
- 桌面 1440×1000 / 1440×900 与手机 390×844 实际浏览器检查：背景完整铺满，桌面后沿约 48%，桌面延伸到左右和底部；左右组件、底部对话和活动选择可操作。
- 男女默认角色及模拟自定义 V2 外观实际显示。检查了写字、招呼、待机、离场与返回的代表画面；招呼验收发现手掌向下，修正为先展开再屈肘竖起手掌。完整动作可在演示页复看；没有把全部 100 组连接都逐帧人工观看。
- 切换教室时，进入过渡的首帧仍为图书馆；离场后才出现教室，随后返回。普通活动与空座保持期间房间、桌面不变。
- 390×844 下页面无横向溢出，功能卡片位于场景下方；纯享退出按钮在 x≈251、y=788、121×42 的可见区域，可正常退出。
- 隔离来源 localhost:3001 通过应用真实文件预览与确认恢复流程验证：V2 透明图集、原照片、头部补身标记、无版本的旧 V1 图集及教室偏好恢复成功；新的 IndexedDB 素材键存在。新页面重新加载后 V2 / V1 均 ready=true，分别走新渲染及旧兼容读取。
- 缺少素材的备份显示“备份缺少角色照片或完整人物素材”，已有角色与素材键不变。恢复前保留的隔离数据已通过同一恢复流程还原。
- 缓存实际只保留 study-buddies-shell-v11；新场景与角色可加载，旧版本缓存移除。
- 功能回归：右侧纪要文字出现在完整纪要工具；场景番茄钟开始、暂停后工具页显示同一暂停会话；结束本轮后恢复开始入口。纪要行动项出现在左侧待办，完成后在日常活动的已完成列表中可见；日历入口和原有次日安排保留。
- 照片接口模拟验证涵盖完整人、头像、无人、多人、无效图集和服务失败；图集契约更新为 V2 4×4，图片请求为 1536×1536。真实图像服务仍未配置，真实照片相似度与生成成功率不计为已验收。
- 临时备份测试页面未进入最终构建；验收代理和标签在结束时关闭。素材与实现说明见 docs/CHIBI-ANIMATION.md。

以下保留之前各轮验收记录作为历史参考。
# 本次本地验收 · 2026-09-06

## 游戏页左右组件与底部对话

- 左侧当日日程与待办、中央人物、右侧番茄钟与纪要。底部对话改为半透明横条，显示角色名、台词、选项、继续与收起；删除“一起专注”快捷入口。人物活动收进折叠面板。
- 桌面检查三栏与底部对话；针对较矮屏幕压缩组件高度，保持对话可见。390×844 手机端人物与对话优先显示，下方左右两组组件无横向溢出。
- 隔离数据实际验证：聊天选项产生回应并可继续；右侧启动/暂停番茄钟后在工具页显示同一会话；纪要输入同步到工具页，测试后恢复原文本；左侧待办勾选后在日常活动的已完成列表显示，再恢复该待办。主预览未写入测试内容。
- 手机纯享模式进入后退出按钮位于可见视口内，点击能正常退出。页面不再存在“一起专注”按钮。已修复完成专注后组件显示 00:00 的问题，现在显示下一轮默认时长。
- 代码规范、类型检查、77 项专项检查与正式构建通过；缓存更新至 v10。人物素材与连续动画引擎保持原实现。

## 日程识别迁移与课件整理

- 日程识别组件移出工具页，日历增加直接入口；日常活动内部完成文字识别、采集/文件/截图导入、编辑预览、确认写入与返回日历。旧书签自动重定向；跨页切换保留未确认预览。
- 工具首项为实际可用的课件整理：PPTX/TXT/MD 导入、文字输入、整理结果、Markdown 导出；资料与纪要、日程输入独立，旧备份缺少该字段仍可恢复。
- 新增 8 项自动检查：路由归属和旧链接兼容、PPTX 幻灯片顺序与 XML 转义、损坏/超限/不支持文件拒绝、本地摘录不编造问题、备份隔离、无密钥、服务失败与模拟 AI 结构化响应。合计 77 项专项通过，代码规范、类型检查及构建通过。
- 浏览器在隔离 localhost:3001 导入两页 PPTX，正确按演示顺序提取文字，实际无密钥整理生成带来源说明的提纲；跨页及刷新后保留资料和结果。迁移后的日程识别仍能预览并确认写入，已有同条日程保持去重，旧链接实际跳转为 #daily/schedule。桌面及 390×844 手机端检查日程识别和课件整理，均无横向溢出；用户 localhost:3000 未写入测试课件。
- 缓存更新至 v9。真实 AI 整理质量未使用有效密钥验收；当前 PPTX 仅提取文字，PDF/图片与旧版 PPT 提供复制文字的替代入口。

## 日历与待办改版验收

- 参考本机旎鹿校园助手的 calendar_page、week_agenda_page、todo_page、年月/时间选择及重复规则源码，并查看其日历宣传截图。移除独立近 7 天统计卡片，日历改为月历在上、当天时间线在下；周视图和待办为独立内页。外层仍为 SBuddy 七页导航。
- 新增日程编辑、年月/日期选择、小时/分钟选择、重复与结束日期、自定义规则、独立待办及提醒编辑。课程的单次隐藏/修改保留其他教学周；课表原始快照可按来源恢复。
- 代码规范、类型检查、校园解析测试通过。SBuddy 共 69 项专项通过，新增 13 项覆盖月历行数、跨年、重复边界与排除日期、闰年、自定义星期及倒数日期、重叠课表排列、压缩时间轴、备份字段保留、单次课程隐藏与跨午夜提醒。正式构建通过。
- 浏览器实际检查桌面与 390×844：月历、时间线、周课表、独立待办和编辑/日期/自定义重复弹层，无横向溢出。已操作年月跳转到 2027 年 1 月及回今天、选择 14:30 自动顺延结束时间、每周重复并在下一周显示、待办截止日期与时间、完成/已完成分类/撤销、刷新恢复。
- 写入测试仅在 localhost:3001 隔离来源；localhost:3000 的原有记录没有用于测试写入。重复日程刷新后仍每个符合日期各出现一次，原有日程和待办保留。
- 缓存更新至 study-buddies-shell-v8；测试确认删除 v7，保留新缓存及其他站点缓存。依赖锁文件、站点配置、访问来源和端口未改变。
- 限制：浏览器使用网页选择器与弹层，系统级通知、真机惯性滚轮和触摸横滑手感未作设备验收；关闭网页不会发送提醒。校历为参考项目内置快照，可选开启，未重新验证学校更新。原教务登录采集与真实 AI/媒体验证限制沿用下文记录。本次不是原生 Flutter 页面逐像素截图一致性认证。

下方是本轮之前的升级及界面精简验收记录。

## 后续界面精简验收

- 主页改为固定宣传内容：不读取当前搭子、日期、日程或默契，固定展示小禾与知序；保留开始游戏和角色入口。
- 删除页头标语、品牌小字、侧栏角落文案、装饰性页副标题和游戏页重复鼓励语；工具说明缩短，权限、备份覆盖等操作提示保留。
- 学习状态移至日常活动折叠面板。浏览器展开后确认日程压力、未记录精力和 10 分钟入口仍可访问。
- 印象改为一段归纳描述；隔离记录输入三条学习习惯后确认不直接复述原文，刷新仍能恢复，删除记忆后描述同步清除。原始内容保留在折叠的管理入口和备份中。
- 新增 3 项印象测试，覆盖去重归纳、否定与删除、角色隔离及最近反馈；总计 56 项专项通过。规范、类型检查及正式构建通过。
- 浏览器查看桌面与 390×844 手机主页；手机日常活动内容宽 380px，未超出视口。主预览仍为 localhost:3000，写入测试使用隔离 localhost:3001。
- 缓存版本更新为 v7。以下保留之前升级阶段的实际验收结果。

本记录来自本次实际执行，不沿用附件中的历史批准或验收结论。仅网页端；未发布站点、未修改 Flutter、未新增账号或云同步。

## 自动检查

- 规范检查：通过，原有 19 项报错已处理。
- TypeScript 类型检查：通过。
- 校园解析夹具：通过，课程 2、考试 1，成绩忽略。
- SBuddy 专项：53 项通过。包括日期跨月/年、日程去重、角色隔离、专注一次性结算、旧版迁移、备份校验、精力缺省值、实时听写重复/迟到回调及权限拒绝。
- 动画专项包含十种循环的端点与固定根位置检查、全部 100 个有序动作切换组合、快速连续切换后的最终状态检查。
- 照片接口用模拟响应覆盖全身照、人头照、无人、多人、错误部件及服务失败；无人/多人不会发出图片生成请求。没有使用真实密钥。
- 缓存升级测试：清理旧版本站缓存、保留其他缓存、排除 AI 接口。浏览器实际读取到 `study-buddies-shell-v6`。
- 正式构建：通过。Vinext 对首页的静态/动态分类仍显示 Unknown，这是当前依赖的分类提示，不影响构建完成。

## 浏览器操作

主预览维持 `http://localhost:3000`，避免改变用户数据来源。写入性测试使用临时 `localhost:3001` 隔离代理，测试数据没有写入用户原预览来源。临时测试页面与代理在验收后移除/停止。

- 桌面与 390×844 手机布局：七页导航可达，手机页面宽度没有超出视口；检查了主页、角色、活动页、日历与工具内容。
- 默认人物：小禾与知序的头发、脸部、眼镜和服装结构不同；已在人物页面和游戏场景实际显示。
- 活动场景：浏览器查看提笔自习、起身离场、返回；桌椅与整体背景固定，离场后人物保持在画面外。人物没有整图摇摆、缩放或溶解切换。循环及其余切换的连续性由上述数值专项覆盖，未逐帧人工观看全部 100 个组合。
- 纯享模式：手机布局进入并通过可见退出按钮返回，七页导航仍可打开。
- 角色隔离：给测试小禾保存印象并完成专注后，切换知序，知序仍为 0 默契且没有该印象。
- 日程：识别“明天 14:00-16:00 图书馆自习”，预览日期为 2026-09-07；两次确认导入后日历仍只有 1 项。验收中修复了时间范围被当成日期片段而污染标题的问题。
- 待办：添加、完成、切换全部、恢复均通过；周/月日历切换通过。
- 专注：实际运行 14 秒后暂停，后续读数保持 24:46；提前结束结算 0.2 分钟并显示 1 条记录。
- 迁移：空隔离来源写入旧日程 1、专注 1、默契 30、精力 4、照片 1。首次加载和再次刷新后数量不变；旧键保留，照片进入 IndexedDB。
- 备份：含照片及人物部件的测试备份通过预览、恢复与刷新；IndexedDB 中出现新的照片/人物键。导出的本地 JSON 实际包含 2 位角色及两项素材。缺失素材的损坏备份被拒绝，当前数据保留。
- 文字纪要：真实输入内容在刷新后恢复，无密钥时生成标明“本地速记”的摘要、要点及行动项。
- 照片入口：隔离浏览器上传遇到服务拒绝页，界面显示“服务返回异常…原人物已保留”，人物未变；这同时验证了非 JSON 错误保护。直接向原地址发送同源 multipart 请求，实际得到 503 和“照片生成人物服务尚未配置”的 JSON 提示。网络异常与超时也有中文提示。

## 待配置或真实设备验证

- 用户明确选择“先完成代码和默认角色，稍后配置服务”。因此真实照片的人物检测准确率、相似度、完整服装重建质量与图片服务成功率尚未验收。代码与模拟接口通过不等于真实生成质量已通过。
- 实时听写的兼容说明和文字替代入口可见，模拟的停止、权限拒绝、重复及迟到回调通过。内置测试浏览器调用原生语音识别后，后续按钮与导航未可靠响应；已关闭该测试标签停止会话。真实麦克风听写、停止按钮、录音和权限拒绝弹窗需要在支持语音识别的真实浏览器继续验收，不能标作设备验收通过。
- 摄像头手势精度、媒体权限实际拒绝、真实音频转写、课表截图 AI 识别和教务登录采集，需要相应设备、有效服务或用户登录；本次未伪造成功结果。

## 回退与配置

升级前 Git 标签：`pre-sbuddy-upgrade-20260905`，对应 `40b1d0ba1776c47ef001ab01e2b97fb5ca30145a`。依赖版本、锁文件、站点与环境配置延用现有项目；仅更新 `.env.example` 的用途注释。使用步骤见 README.md。

