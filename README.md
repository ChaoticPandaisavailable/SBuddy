# 完蛋！我被学习搭子包围了 / SBuddy

个人校园学习网页：日历、待办与专注记录共享，每位像素搭子独立保存外观、默契、对话、印象和成长奖励。当前交付为本地版本，无账号、云同步或公开部署。

## 本地运行

需要 Node.js 22.13 或更高版本。

```powershell
npm ci
Copy-Item .env.example .env.local
npm run dev
```

打开终端显示的网址（默认 http://localhost:3000）。已有 .env.local 时不要覆盖它。首次启动会准备 MediaPipe 模型与 WASM，需网络下载；之后手势推理在浏览器运行。

```powershell
npm run lint
npm run test:campus
npm run test:sbuddy
npm run build
```

保留了原有 React、Vinext、Vite 和 Sites 配置。构建命令不发布网站。构建产物可用 npm start 本地预览（端口以 Wrangler 输出为准）。不同端口属于不同浏览器存储来源。

## 七页使用

- **主页**：校园陪伴主视觉、当前搭子、开始游戏及日历入口。
- **开始游戏**：依据实际日程切换上课、自习、会议和空闲动画；聊天和完成专注积累默契。纯享模式支持退出按钮与 Esc；浏览器不允许全屏时仍可进入页面内纯享布局。
- **角色**：预设小禾、知序、向阳，以及可创建、命名、换装、照片生成造型和删除的自建搭子。对话偏好、专注反馈和“你告诉 TA 的”印象分开展示。照片生成像素配色与造型，保留原有动画骨架。
- **日常活动**：周/月日历、日期详情、手动安排、课程、考试；右侧待办支持编辑、完成、恢复和删除。设置学期起始日后，课程按周次与节次映射到日期。提醒仅在页面运行期间出现。
- **工具**：日程识别、纪要、手势识别、番茄钟。切换页面保留编辑草稿与计时状态。番茄钟支持 10/25/45 分钟、自定义、暂停、继续、重置和结束；以时间戳计时，结算归属于开始时的搭子。结束后可以记录感受。
- **鉴赏**：默契 25/50/75 各解锁两项昵称、回应、动作或特别对话；回看不重复奖励。
- **设置**：音效、减少动画、默认专注时长、学期设置、备份恢复、演示数据与清空数据。新用户默认没有虚构学习记录。

## 日程识别与校园采集

入口统一在 **工具 → 日程识别**，遵循“识别 → 编辑预览 → 确认写入 → 查看日历”。

文字日程根据实际参考日期解析今天、明天、下周等；不明确的日期和起止时间必须补全。重复确认相同安排会去重。文本文件与校园课表/考试的 HTML、TXT、JSON、CSV 和截图导入集中在此处。

校园采集复用原来的数智人大采集桥，只接受指定官方站点 jw.ruc.edu.cn。普通网页无法跨域读取登录后的教务内容：

1. 在“课表与考试 · 采集 / 文件 / 截图”中按说明复制采集代码、创建书签。
2. 从本站打开官方课表或考试页面，由用户自己登录。
3. 点击采集书签，将页面可见课表/考试表格回传预览；也可手动粘贴或上传文件。
4. 校对名称、周次/节次、日期、时间和地点，再确认写入。
5. 在设置中核对学期起始日，到日历查看结果。

不接收教务密码，不采集成绩。学校页面结构变化时可能需要调整解析器；真实登录采集需要用户在自己的浏览器完成。

## AI 与媒体

在服务端 .env.local 配置 OPENAI_API_KEY，不能放入 NEXT_PUBLIC_* 变量。模型字段见 .env.example，可按自己的服务权限调整：

- OPENAI_TEXT_MODEL：日程、纪要、对话。
- OPENAI_VISION_MODEL：照片造型分析和课表截图识别。
- OPENAI_TRANSCRIBE_MODEL：音频转写。
- OPENAI_IMAGE_MODEL：保留原可选图片接口，当前固定骨架换肤不依赖它。

无密钥时，文字日程规则解析、规则对话、照片本地配色分析、手动造型与文字速记摘要可用；结果标明本地规则来源。截图识别、音频转写依赖服务，失败会显示原因并保留文本替代入口，不生成虚构识别内容。

摄像头和麦克风只在主动点击后请求权限；离开对应工具会停止媒体流。录音可在当前会话下载；原始音频不会持久保存。转写文字与纪要会保存，可选择行动项加入待办。

## 数据、迁移与备份

- 结构化数据保存在当前来源的 localStorage（sbuddy-state-v2），照片素材保存在 IndexedDB（sbuddy-assets）。
- 所有学习数据属于用户；外观、关系与对话按稳定搭子 ID 隔离。异步对话、照片与专注结算按发起时的搭子写回。
- 第一次发现旧版单角色数据且没有新版数据时迁移到小禾；旧键原样保留。旧版无年份日程按原项目 2026 年背景迁移，新增日程不使用固定年月。旧个人资料原件也存档到新版数据的 legacyProfile。
- 导出备份包含全部角色、学习记录、草稿及照片；恢复先校验与预览，再由用户确认覆盖。恢复会替换当前结构化数据，建议先备份。
- 浏览器禁止存储、空间不足或数据损坏时页面明确提示。隐私浏览、清理浏览器数据、换浏览器或换端口可能造成记录不可见或丢失。
- 演示数据从设置手动载入并标示；不会在初始化时覆盖真实数据。
- 清空数据需要明确确认，包含旧数据和照片。关闭页面后的推送不在本地版本范围内。

## 代码导航

- components/sbuddy/provider.tsx：共享状态、持久化、专注结算、运行中提醒。
- components/sbuddy/app.tsx：七页导航、首页、游戏、鉴赏与共享组件。
- components/sbuddy/{characters,daily,tools,settings}.tsx：功能页面。
- lib/sbuddy-state.ts / sbuddy-storage.ts：版本数据、迁移、校验、去重、IndexedDB。
- lib/schedule-parser.ts / schedule-engine.ts：实际日期解析与日程状态。
- components/campus-calendar-workspace.tsx：复用的课程、考试和采集桥。
- lib/campus-data.ts / campus-parser.ts：校园数据与解析。
- lib/relationship.ts：原默契规则、问题与解锁。
- components/pixel-companion-canvas.tsx：原像素素材动画及造型渲染。
- app/api/ai/：兼容旧调用的服务端接口；日程增加参考日期/时区，对话增加搭子名/性格。

设计决策见 DESIGN.md、PRODUCT.md、direction-approved.md。验证记录见 VALIDATION.md。
